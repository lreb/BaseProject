# Example environment variables configuration for deployment
# IMPORTANT: DO NOT commit the .env file with real values
# Copy this file as .env and configure your values

# Server information
DEPLOY_SERVER_IP=192.168.100.142
DEPLOY_SERVER_USER=your_username
DEPLOY_REMOTE_PATH=/var/www/base-angular-app
DEPLOY_SERVER_PORT=4200

# Usage example:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your real values
# 3. Load variables before deploying:
#    PowerShell: Get-Content .env | ForEach-Object { $var = $_.Split('='); [Environment]::SetEnvironmentVariable($var[0], $var[1], 'Process') }
#    Bash: export $(cat .env | xargs)
